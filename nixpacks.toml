[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install --upgrade pip && pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python implementation/api_server.py"

[statics]
# No static assets

[[statics.glob]]
from = "implementation"
to = "implementation"
